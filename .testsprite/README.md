# TestSprite Workspace

## Purpose

This directory is reserved for future TestSprite configuration, notes, and run artifacts that are safe to commit.

## Usage

Once the app is deployed, use TestSprite CLI to verify the public app URL and document meaningful results in `LOOP.md`.

Use `runbook.md` for the repeatable verification procedure.

Do not commit secrets, private account tokens, or large generated artifacts.
